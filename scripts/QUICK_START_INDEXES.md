# Быстрый старт: Применение индексов производительности

## 🚀 Быстрое выполнение в pgAdmin

1. **Откройте pgAdmin**
2. **Подключитесь** к серверу PostgreSQL
3. **Найдите базу данных** `kurs_sport_shop`
4. **Кликните правой кнопкой** на базе данных → выберите **Query Tool**
5. **Откройте файл** `scripts/apply_performance_indexes.sql`
6. **Нажмите Execute** (F5) или кнопку ▶️

## ✅ Что произойдет

Скрипт:
- ✅ Создаст все необходимые индексы
- ✅ Проверит существующие индексы (не создаст дубликаты)
- ✅ Обновит статистику для оптимизатора
- ✅ Покажет список всех созданных индексов

## 📋 Результат

После выполнения вы увидите список всех индексов в базе данных, отсортированных по таблицам.

## ⚠️ Важно

- Скрипт можно запускать **многократно** - он не создаст дубликаты
- Индексы **безопасны** - они только ускоряют запросы
- Время выполнения: **1-2 минуты** в зависимости от размера БД

## 🔍 Проверка

После выполнения проверьте индексы:

```sql
SELECT COUNT(*) as total_indexes 
FROM pg_indexes 
WHERE schemaname = 'shop';
```

Ожидаемое количество: **~30-35 индексов** (включая существующие)

---

**Готово!** Индексы применены, производительность БД улучшена! 🎉


